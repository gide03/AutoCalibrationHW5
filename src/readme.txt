1. Install python dependecies with `pip install -r requirements`
2. Configure serial connections in `config.py` file
3. Enter command prompt interface and go to src directory
4. Run calibration script `python calibration_v2.py`